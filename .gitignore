# Upstream WLED checkout (git clone https://github.com/wled/WLED WLED). It is a
# separate repository and must never be vendored here (see CLAUDE.md).
WLED/

# Boot files fetched on demand by bin/flash.sh
.cache/
