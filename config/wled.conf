# WLED release this project builds against: a git tag (or branch/commit) in
# https://github.com/wled/WLED. bin/check-env.sh clones/checks this version
# and bin/build.sh warns when the WLED/ checkout is at something else.
# Latest stable as of 2026-08-30.
WLED_VERSION=v16.0.1
